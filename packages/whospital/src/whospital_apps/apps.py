"""Application configuration for whospital_apps."""

from django.apps import AppConfig


class WhospitalAppsConfig(AppConfig):
    """App config for the whospital_apps Django app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "whospital_apps"
